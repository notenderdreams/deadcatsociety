services:
  - type: web
    name: limi-backend
    runtime: python
    region: oregon
    plan: free
    buildCommand: ""
    startCommand: uvicorn src_limi.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.10"
